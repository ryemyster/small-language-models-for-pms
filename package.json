{
  "name": "small-language-models-for-pms",
  "version": "0.1.0",
  "description": "Build a small language model as a PM — hands-on tutorial, no ML background required.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "classify": "tsx src/classify.ts",
    "compare": "tsx src/compare.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "tsx": "^4.11.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
